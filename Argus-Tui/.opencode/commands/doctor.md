---
description: Run comprehensive health checks on the Argus runtime
---
I need to run Argus health checks. I should use the `bash` tool to execute:

```bash
argus doctor
```

This checks: Python runtime, MCP worker, Playwright, database, credentials, LLM config, and toolchain.
