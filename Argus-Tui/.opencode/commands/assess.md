---
description: Run a full autonomous security assessment against a target URL
---
I need to run the Argus security assessment CLI. I should use the `bash` tool to execute:

```bash
argus assess $ARGUMENTS
```

This runs the full assessment pipeline via the MCP worker.
