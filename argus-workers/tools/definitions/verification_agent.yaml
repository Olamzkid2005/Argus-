name: verification_agent
command: python3
args:
  - argus-workers/tools/run_agent_tool.py
  - verification_agent
description: "verification_agent - security assessment tool"
parameters:
  - name: target
    type: string
    description: "Target URL or scope"
    required: true
    flag: "--target"
  - name: extra
    type: string
    description: 'JSON-encoded extra parameters (e.g. {"tech_stack":["apache"]})'
    required: false
capabilities:
  - security_analysis
signal_quality: CONFIRMED
risk_level: low
priority: 90
cost: low
enabled: true
timeout: 300
