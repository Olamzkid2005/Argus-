name: s3scanner
command: s3scanner
description: "Amazon S3 bucket discovery and enumeration"
args:
  - "--json"
parameters:
  - name: target
    type: string
    description: "Bucket name or domain"
    required: true
capabilities:
  - web_recon
signal_quality: CANDIDATE
priority: 35
cost: low
enabled: true
timeout: 180
