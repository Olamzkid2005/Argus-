name: cloud_enum
command: cloud_enum
description: "Cloud asset enumeration tool (AWS, Azure, GCP)"
args:
  - "--json"
parameters:
  - name: target
    type: string
    description: "Target domain or keyword"
    required: true
    flag: "-k"
capabilities:
  - web_recon
  - cloud_enum
signal_quality: CANDIDATE
risk_level: low
priority: 30
cost: low
enabled: true
timeout: 300
