name: "commix"
command: "commix"
description: "Command injection detection"
args: ["--batch"]
enabled: true
timeout: 300
parameters:
  - name: "target"
    type: "string"
    description: "Target URL"
    flag: "--url"
    required: true
