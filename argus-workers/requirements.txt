# Argus Workers - Python Dependencies

# Task Queue
celery[redis]==5.4.0
redis==5.2.1

# Database
# psycopg2-binary avoids the libpq-dev build dependency.
# Trade-off: may segfault under libpq version mismatch and lags security patches.
# Switch to psycopg2 if deploying in a controlled environment with libpq-dev available.
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# HTTP Clients
httpx==0.28.1
requests==2.32.3

# AI/LLM
openai==1.59.5
anthropic==0.42.0

# Security Tools Integration
python-nmap==0.7.1

# Data Processing
pydantic==2.10.4
python-dotenv==1.0.1
defusedxml==0.7.1

# Templating (Compliance Reports)
jinja2==3.1.5

# Encryption (settings/credentials)
cryptography==44.0.2

# Process management (orphaned subprocess cleanup)
psutil==6.1.0

# Logging and Monitoring
structlog==24.4.0

# OpenTelemetry
opentelemetry-api==1.20.0
opentelemetry-sdk==1.20.0
opentelemetry-exporter-otlp-proto-http==1.20.0

# M-35: Test dependencies moved to requirements-dev.txt to avoid bloating
# production images with ~400MB of browser binaries.
# Install with: pip install -r requirements-dev.txt
# pytest==8.3.4
# pytest-asyncio==0.25.2
# pytest-celery==1.1.2
# pytest-cov==6.1.1
# playwright>=1.59.0

# HTML Parsing (form discovery for auth)
beautifulsoup4==4.12.3
lxml==5.3.0

# Docker SDK (sandbox container isolation)
docker>=7.0.0

# PDF report generation
fpdf2>=2.7.0

# WebSocket scanning
websockets==12.0
